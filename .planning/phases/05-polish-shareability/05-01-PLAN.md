---
phase: 05-polish-shareability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/filters.js, src/style.css, src/index.html]
autonomous: true

must_haves:
  truths:
    - "The floating filter control has small German labels ('Bereich', 'Beruf') above each dropdown"
    - "All interface text is in German Einfache Sprache (dropdown defaults, labels, no-results message)"
    - "Filter functionality (sector/profession dropdowns, no-results message) works identically to Phase 4"
    - "The page title is in German"
  artifacts:
    - path: "src/filters.js"
      provides: "Updated createFilterControl with German labels above each dropdown"
      contains: "Bereich"
    - path: "src/style.css"
      provides: "Label styling for filter control"
      contains: "filter-label"
    - path: "src/index.html"
      provides: "German page title"
      contains: "PrA"
  key_links:
    - from: "src/filters.js"
      to: "src/style.css"
      via: "filter-label class used on label elements"
      pattern: "filter-label"
---

<objective>
Add small German labels above each dropdown in the floating filter control and ensure all interface text uses German Einfache Sprache.

Purpose: Deliver UX-02 (German plain language). The existing Leaflet L.Control filter overlay is kept as-is — just enhanced with labels and German text polish. No layout restructuring needed.

Output: Filter control with labeled dropdowns, all text in German Einfache Sprache.
</objective>

<execution_context>
@/Users/oetiker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oetiker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-shareability/05-RESEARCH.md
@.planning/phases/04-filtering/04-01-SUMMARY.md
@src/index.html
@src/main.js
@src/filters.js
@src/map.js
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add German labels to filter control and polish all text to Einfache Sprache</name>
  <files>src/filters.js, src/style.css, src/index.html</files>
  <action>
**src/filters.js** — Update `createFilterControl` to add small labels above each dropdown:

1. In the `onAdd` method, before the sector `<select>`, create a label element:
   ```javascript
   const sectorLabel = L.DomUtil.create('label', 'filter-label', container);
   sectorLabel.textContent = 'Bereich';
   ```

2. Before the profession `<select>`, create a label element (initially hidden, shown/hidden together with the profession select):
   ```javascript
   const profLabel = L.DomUtil.create('label', 'filter-label', container);
   profLabel.textContent = 'Beruf';
   profLabel.style.display = 'none';
   ```

3. When showing/hiding the profession dropdown, also show/hide its label:
   - On sector change when a sector is selected: `profLabel.style.display = '';`
   - On sector change when cleared: `profLabel.style.display = 'none';`

4. Return the select elements for external access (needed by Plan 05-02 for hash state restoration):
   Modify `createFilterControl` to accept a third parameter or return the control instance that exposes `sectorSelect` and `profSelect`. The simplest approach: store references on the control instance so they can be accessed after `.addTo(map)`:
   ```javascript
   const control = new FilterControl();
   control.sectorSelect = null;  // set in onAdd
   control.profSelect = null;    // set in onAdd
   return control;
   ```
   In `onAdd`, assign: `this.sectorSelect = sectorSelect; this.profSelect = profSelect;`

5. Verify all existing German text is correct Einfache Sprache:
   - "Alle Bereiche" (default sector option) — good, keep
   - "Alle Berufe" (default profession option) — good, keep
   - Profession display strips "PrA " prefix — good, keep

**src/style.css** — Add label styling:
```css
.filter-label {
  font-size: 11px;
  font-weight: 600;
  color: #555;
  margin-bottom: -4px;
}
```
This creates small, subtle labels above the dropdowns within the existing `.filter-control` container.

**src/index.html** — Update the page title:
- Change `<title>` from "INSOS PrA Map" to "PrA Ausbildungsplätze – INSOS Karte"

**src/main.js** — Verify German text:
- The no-results message "Keine Anbieter für diese Auswahl gefunden." is already good Einfache Sprache
- If there's an error catch block with English text, update to German: "Die Daten konnten nicht geladen werden."
  </action>
  <verify>
Run `npx vite build` -- should succeed with no errors. Verify:
1. `src/filters.js` contains 'Bereich' and 'Beruf' label creation
2. `src/style.css` contains `.filter-label` class
3. `src/index.html` title is in German
4. No English-language user-facing strings remain
  </verify>
  <done>
Floating filter control has small "Bereich" and "Beruf" labels above each dropdown. All interface text is German Einfache Sprache. Page title updated. Filter functionality unchanged.
  </done>
</task>

</tasks>

<verification>
1. `npx vite build` succeeds without errors
2. Filter control shows "Bereich" label above sector dropdown
3. Filter control shows "Beruf" label above profession dropdown (when visible)
4. Profession label hides/shows together with its dropdown
5. All visible UI text is German Einfache Sprache
6. Page title is German
7. Filter functionality identical to Phase 4
</verification>

<success_criteria>
- Labels visible above each dropdown in the floating control
- All interface text is German Einfache Sprache
- Filter functionality unchanged from Phase 4
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-shareability/05-01-SUMMARY.md`
</output>
