# CLAUDE.md

## Workflow

This project uses the GSD (Get Shit Done) workflow for planning and execution. Use `/gsd:progress` to check status and `/gsd:quick` for small tasks.

## After Changes

Always push to `main` after committing. A GitHub Actions workflow automatically rebuilds and deploys to GitHub Pages on every push.
